#include "area.h"

float shapes::area::calculateSquareArea(const square& param){
    return square.GetLength() * square.GetLength();
}

float shapes::area::calculateTriangleArea(const triangle& paramtwo){
    return 0.5 * triangle.GetBase() * triangle.GetHeight();
}
float shapes::area::calculateCircleArea(const circle& paramthree){
    return 
}